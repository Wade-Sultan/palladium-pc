# Turns are arriving faster than the workers can run them.
#
# THE SYMPTOM USERS ACTUALLY FEEL. A backlog does not error, every turn still
# completes and still persists, it just adds queue time in front of a build
# that already takes up to 180 seconds. The user experiences it as the app
# getting slow, with nothing in the API's own latency metrics to show for it,
# because builder answers POST /chat immediately either way.
#
# BACKLOG DEPTH IS THE WEAKER SIGNAL and comes second here for a reason: 20
# queued turns is fine if they drain in a minute and terrible if they do not.
# Oldest-unacked-age measures the wait directly, which is why it gets the
# tighter threshold.
displayName: "Palladium. Chat turn backlog"
documentation:
  mimeType: text/markdown
  content: |
    Turns are queuing on `chat-turns-workers` faster than the worker pool drains
    them. Builds still complete; they just wait first.

    **Check current capacity:**

    ```
    kubectl -n palladium get pods -l app=worker
    kubectl -n palladium logs -l app=worker --tail=50 | grep -c 'published\|turn '
    ```

    **Raise concurrency before raising replicas.** Turns are blocked on
    OpenRouter, not on CPU, so a worker at `PUBSUB_MAX_CONCURRENCY=8` is mostly
    idle. Doubling the setting in
    `deploy/overlays/prod/patches/config-prod.yaml` costs nothing on Autopilot;
    doubling replicas costs a pod. The ceiling is memory (each in-flight turn
    holds a DSPy state) and OpenRouter's own rate limits, not cores.

    **If concurrency is already high**, the answer is more replicas, which KEDA
    is already doing, up to `maxReplicaCount: 8`
    (`deploy/overlays/prod/keda-worker.yaml`). A backlog that persists while the
    pool sits below that ceiling means the scaler is not reacting, not that it
    needs a bigger one: `kubectl -n palladium describe scaledobject worker`.

    **Rule out a cold start first.** The pool floors at 0, so a burst arriving
    into an idle cluster queues for ~25-35s before the first pod pulls. Longer
    if Autopilot has to provision a node for it. That is a slow first turn, not
    congestion, and it clears itself. Both conditions below are duration-gated
    well past it.

    **If a cold start is taking minutes rather than seconds**, the fast wake-up
    trigger is not firing and the pool is falling back to the Cloud Monitoring
    one. Check that the `valkey-wake` trigger resolved its connection details
    from the worker's env: `kubectl -n palladium describe scaledobject worker`.
    Compare `LLEN chat:wake` against what the scaler reports. A list that grows
    and never drains means workers are not reaching `_handle`, not that the
    scaler is broken.

    **A sudden backlog with no traffic change** usually means the workers are
    not pulling at all: check for `Valkey unreachable` or a crash loop before
    scaling anything.
combiner: OR
conditions:
  - displayName: "oldest queued turn is waiting too long"
    conditionThreshold:
      filter: >-
        resource.type = "pubsub_subscription"
        AND resource.labels.subscription_id = "chat-turns-workers"
        AND metric.type = "pubsub.googleapis.com/subscription/oldest_unacked_message_age"
      comparison: COMPARISON_GT
      # 120s of queue time, on top of a build that can legitimately take 180s.
      # Deliberately well below the 600s ack deadline: past that a turn is
      # redelivered, and by then the user has almost certainly given up.
      #
      # THE 300s DURATION IS NOW load-BEARING, not padding. With the worker
      # pool floored at 0, a turn arriving into an idle cluster waits out the
      # whole 0->1 path (Cloud Monitoring's 60-180s metric delay, then polling,
      # scheduling and start), so this metric crossing 120s is the *expected*
      # shape of a cold start rather than evidence of congestion. Five minutes
      # is longer than that path's typical worst case, so a cold start resolves
      # itself before the condition matures. Shortening this without also
      # shortening the wake-up would trade a real alert for a recurring one.
      thresholdValue: 120
      duration: 300s
      trigger:
        count: 1
      aggregations:
        - alignmentPeriod: 60s
          perSeriesAligner: ALIGN_MAX
          crossSeriesReducer: REDUCE_MAX

  - displayName: "backlog depth is growing"
    conditionThreshold:
      filter: >-
        resource.type = "pubsub_subscription"
        AND resource.labels.subscription_id = "chat-turns-workers"
        AND metric.type = "pubsub.googleapis.com/subscription/num_undelivered_messages"
      comparison: COMPARISON_GT
      # 32 was picked as two full rounds behind a warm floor of 2 replicas x 8
      # concurrent = 16 in flight. That floor is now 0 (keda-worker.yaml), so
      # the number no longer derives from a standing capacity, but it is kept,
      # because what it measures is unchanged: 32 queued turns is far more than
      # any single cold start can explain, and KEDA asks for ceil(32/8) = 4
      # replicas long before this matures. Reaching it for ten minutes means the
      # scaler is not getting what it asked for.
      thresholdValue: 32
      duration: 600s
      trigger:
        count: 1
      aggregations:
        - alignmentPeriod: 60s
          perSeriesAligner: ALIGN_MAX
          crossSeriesReducer: REDUCE_MAX
alertStrategy:
  # Six hours. Backlog is self-resolving once traffic drops, and a stale open
  # incident from last night's spike is noise.
  autoClose: 21600s
enabled: true
notificationChannels: []
